<% unless @text.blank? %>
  <%= textilizable @text, :attachments => @attachments, :object => @previewed %>
  <script type="text/javascript">MathJax.Hub.Queue(["Typeset",MathJax.Hub,"#preview"]);</script>
<% else %>
  <p class="empty-preview"><%= l(:label_nothing_to_preview) %></p>
<% end %>
